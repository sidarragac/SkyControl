// Developed by Juan Jara
// Internal imports
import type { Country } from '@/types/SharedTypes';

export type MapData = {
  country: Country;
  airlineCount: number;
  lat: number;
  lng: number;
};
